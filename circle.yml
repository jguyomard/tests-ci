machine:
  php:
    version: 7.0.3

dependencies:
  override:
    - uname -a
    - lsb_release -a
    - date
    - ls -la
    - pwd
    - id
    - printenv
    - composer --version

test:
  override:
    - php -v

